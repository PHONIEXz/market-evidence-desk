# Hyperframes composition brief

Create a 21-second, 1280 × 720 landscape Brag video for Market Evidence Desk in `composition/`. Follow `brag-plan.md`. Exact source copy is drawn from `web/index.html`, `web/research.html`, `web/compare.html`, and `web/atlas.html`. Reconstruct recognizable interface cards in HTML using the site's palette, typography, and source-trail logo. Do not fetch live data or imply that a live agent response is playing. The example question is the site's own reserve-snapshot comparison. Display its boundary: customer inclusion is narrower than overall-liability assurance. Keep the outro disclaimer legible. Quiet original instrumental music at low volume, no narration. A small number of soft motion-matched accents. All animation must be deterministic and seek-safe; one paused root GSAP timeline, clipped timed scenes, local audio. Lint and perform a full browser check before rendering; export MP4, poster, and share copy.
